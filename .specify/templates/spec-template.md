# Feature Spec — [feature-id]

> Generated by `/speckit.specify`. Lock-once: changes produce a new feature ID.

## Business principle traced

Pick exactly one. Spec is rejected at `/speckit.analyze` if absent.

- [ ] Conversion
- [ ] Consideration
- [ ] Retention

## Problem statement

What user-facing problem does this Feature solve? One paragraph, plain language.

## Business outcome (goal-level AC)

The outcome the assembled product must deliver. Tested by **Playwright** end-to-end (Crank constitution §test-ladder).

- Given …
- When …
- Then …

## Story decomposition

A Feature decomposes into **Stories**. Every Story is component-shaped — testable as a single unit in **Storybook**. Page-assembly Stories declare dependencies on Component Stories.

| Story ID | Type | Depends on | Storybook test surface |
|----------|------|------------|------------------------|
| S-001 | Component | — | functionality / theme / W3C a11y |
| S-002 | Page assembly | S-001, … | composition / theme integrity at assembled level |

## Out of scope

What this Feature explicitly does **not** address. Forces sibling Features rather than scope creep.

## Dependencies

Other Features that must ship first. Lock-once means dependency direction is fixed at spec time.

## Source references

Where this spec came from — Notion ticket, Figma frame, stakeholder ask. Non-empty; agents reject specs without provenance.
