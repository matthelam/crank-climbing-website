# .claude

Claude Code configuration and operational assets for this repository.

- [`settings.json`](settings.json) — permissions, hooks, model preferences.
- `settings.local.json` — per-user overrides (gitignored).
- [`skills/`](skills/) — custom skills (Agent and Instruction definitions; daisy-chain target from the root README).
- [`agents/`](agents/) — subagent definitions.
- [`commands/`](commands/) — custom slash commands.

**Status: placeholder.** Each sub-area is to be populated as needed — see [root README → TODO](../README.md#todo).
