# v0

**Status: *[TBD]*** — scaffolding only.

Detail page for the Standard Platform **v0** row — Signal: "Generating UI from a prompt or Figma frame".

## Planned contents

- When to reach for v0 versus authoring components by hand.
- Expected output conventions — shadcn/ui + Tailwind, matching the rest of the stack.
- Lineage: how a v0 generation becomes committed source under `src/`, and what evidence links it back to the originating Figma frame.
- Interaction with **Figma** and **Radix / shadcn/ui**.

Body to be elaborated once the first v0-sourced component lands.
