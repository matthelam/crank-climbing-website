# Playwright MCP

**Status: *[TBD]*** — scaffolding only.

Detail page for the Standard Platform **Playwright MCP** entry — Signal: "Automating a browser (scripted interactions)".

## Planned contents

- Install and configuration for agents.
- Boundary versus **Playwright** — the MCP server drives a browser for agent interaction; the Playwright test framework verifies UI behaviour in CI.
- Safe-use guidance — deterministic selectors, avoiding hidden side effects.
- Interaction with **Chrome DevTools MCP** for diagnostic inspection.

Body to be elaborated once MCP usage stabilises.
