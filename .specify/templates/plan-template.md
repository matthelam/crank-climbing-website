# Implementation Plan — [feature-id]

> Generated by `/speckit.plan` from `spec.md`. Lock-once.

## Standard Platform tools selected

From the Crank constitution Standard Platform table. Pick only what this Feature needs — no speculative additions.

| Concern | Tool |
|---------|------|
| Page/route | Next.js |
| Styling | Tailwind |
| Primitives | Radix via shadcn/ui (list specific primitives) |
| Component dev/test | Storybook |
| End-to-end test | Playwright |
| (other) | … |

If a need arises that no Standard Platform tool covers, **stop**. Do not introduce a new dependency in `/speckit.plan`. Surface to the human; constitution amendments are out-of-band.

## Architecture

How this Feature sits in the existing app — routes, server boundaries, data flow, shared state. Cite existing files where the work attaches.

## Component inventory

Every Component Story this Feature requires, atomic-design-classified. This list seeds `/speckit.tasks`.

| Component | Atomic level | New / existing | Story file path |
|-----------|--------------|----------------|-----------------|
| `Button` | atom | existing | `src/components/atoms/Button.stories.tsx` |
| … | | | |

## Page assemblies

Page-level Stories that compose the components above.

| Page Story | Composes | Route |
|------------|----------|-------|
| `MembershipPage` | `Button`, `PriceCard`, … | `/memberships` |

## Test layer routing

| Story type | Framework | Test file pattern |
|------------|-----------|-------------------|
| Component Story | Storybook | `*.stories.tsx` with failing `play` |
| Page assembly Story | Storybook | `*.stories.tsx` (composition + theme integrity only) |
| Feature business process | Playwright | `tests/e2e/[feature-id].spec.ts` |
| Non-UI Task | Vitest | `*.test.ts` |

## Sequencing

Recursive Build order — Storybook-green precedes Playwright-green (constitution §test-ladder). List Stories in dependency order so `/speckit.tasks` produces a valid build graph.

## Rejected alternatives

Tools/approaches considered and discarded with one-line reason. Forces honest decision-making.
