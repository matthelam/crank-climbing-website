# TypeScript

**Status: *[TBD]*** — scaffolding only.

Detail page for two Standard Platform rows — **TypeScript** (Signal: "Writing typed code") and **TypeScript strict** (Signal: "Enforcing type correctness").

## Planned contents

- `tsconfig.json` baseline, including strict-mode flags.
- Import path aliases and module boundaries.
- Conventions for shared types, domain models, schema-derived types.
- CI type-check configuration.

Body to be elaborated once the first typed module lands.
