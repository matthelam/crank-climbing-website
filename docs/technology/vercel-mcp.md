# Vercel MCP

**Status: *[TBD]*** — scaffolding only.

Detail page for the Standard Platform **Vercel MCP** entry — Signal: "Looking up framework or Vercel documentation".

## Planned contents

- Install and configuration for agents.
- What the MCP server surfaces — deployment state, docs, project inspection.
- Interaction with **Vercel** (the platform itself) and **Next.js DevTools MCP**.
- Boundary: read-only lookups only; writes to Vercel go through the platform, not the MCP.

Body to be elaborated once MCP usage stabilises.
