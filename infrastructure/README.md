# infrastructure/

Delivery-surface configuration that isn't application code — deployment manifests, environment definitions, IaC when it lands.

Placeholder. Expect to host Vercel project config, environment variable specifications, and any future Terraform or Bicep when the system grows beyond Vercel's defaults. CI workflows remain under [`.github/workflows/`](../.github/) because GitHub requires that location.

Nothing lives here yet — the directory exists to make the delivery bones visible at the root.
